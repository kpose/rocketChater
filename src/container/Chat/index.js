import React from "react";
import { View, Text, SafeAreaView } from "react-native";

const Chat = ({ route, navigation }) => {
  return (
    <SafeAreaView>
      <Text>Chat</Text>
    </SafeAreaView>
  );
};

export default Chat;
